---
"@everything-dev/auth-plugin": patch
---

Improve the auth.everything.dev demo navigation and capability pages so SIWN, relayer signing, profile lookup, account linking, auth methods, API keys, organizations, and admin user management are easier to discover.
